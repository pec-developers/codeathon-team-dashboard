spring.application.name=codeathon-team-dashboard-backend
server.port=8080

spring.datasource.url=${DATASOURCE_URL:jdbc:postgresql://localhost:5432/codeathon_team_dashboard}
spring.datasource.username=${DATASOURCE_USERNAME:postgres}
spring.datasource.password=${DATASOURCE_PASSWORD:password}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

jwt.secret=${JWT_SECRET:dGhpcyBpcyBhIHNlY3JldCBrZXkgZm9yIGp3dCAxMjM0NTY=}
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

admin.username=${ADMIN_USERNAME:admin}
admin.password=${ADMIN_PASSWORD:admin123}
